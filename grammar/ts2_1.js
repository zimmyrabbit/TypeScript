"use strict";
const str = 'hello';
const num = 123;
const bool = false;
const n = null;
const u = undefined;
const sym = Symbol('sym');
const big = 100000000000n;
const obj = { hello: 'world' };
function plus(x, y) {
    return x + y;
}
;
const minus = (x, y) => x - y;
